# Ganti dengan alamat IP LAN komputer kamu yang menjalankan backend,
# BUKAN "localhost" (HP/emulator tidak bisa akses localhost milik komputer).
# Contoh: kalau backend jalan di http://192.168.1.10:5000
EXPO_PUBLIC_API_URL=http://172.31.144.1:5000/api
